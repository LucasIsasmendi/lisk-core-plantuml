@startuml
class Peer < logic > {
  .. prototype ..
  + properties
  + immutable
  + headers
  + nullable
  -- Methods --
  + Peer (peer)
  + accept (peer)
  + normalize (peer)
  + parseInt (integer, fallback)
  + applyHeaders (headers)
  + update (peer)
  + object ()
}
@endtuml
